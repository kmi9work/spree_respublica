# Installation

* You need Imagemagick.
* Clone
* Run `bundle install`
* Run next code:

```bash
rake db:create
rake db:migrate
rake db:seed
rake spree_sample:load
rake db:seed:load_data
```

*Run `rails s`
